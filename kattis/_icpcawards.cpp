#include <bits/stdc++.h>
#include <stdio.h>

#define PI 3.14159

int main(){
    using namespace std;
    string str, buffer;
    vector<string> buff;
    int n;
    cin>>n;
    cin.ignore();
    for(int i=0; i<n; i++){
        getline(cin, str); 
        buff.push_back(str);
        
    }



    return 0;
}   