#include <bits/stdc++.h>
using namespace std;

#define ull unsigned long long
#define ll long long
#define koma(x) fixed<<showpoint<<setprecision(x)

void solve(){

    int n, x;
    str
    vector<string> phone;
    string str;
    cin>>n;
    for(int i=0; i<n; i++){
        phone.clear();
        cin>>x;
        for(int j=0; j<x; j++){
            cin>>str;
            phone.push_back(str);
        }

        for(int j=0; j<)
        
    }

    

}

int main(){
    ios_base::sync_with_stdio(0);
   
    solve();
    

    return 0;
}   